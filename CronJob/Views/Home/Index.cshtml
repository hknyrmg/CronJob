@model IEnumerable<CronJob.Data.Entities.Demo>
@{
    ViewData["Title"] = "Home Page";
}

<meta http-equiv="refresh" content="3" ; url="http://localhost/Home/n">

<b style="color:brown">itemCount => </b> <b>@Model.Count().ToString()</b>
@*@foreach (Demo item in Model)
    {
        <table>
            <tr>

                <td>
                    @item.DemoId
                </td>
                <td>
                    @item.DemoName
                </td>

                <td>
                    @item.CreatedTime
                </td>
            </tr>
        </table>
    }*@
@foreach (var item in Model)
{
    <p style="color:blueviolet">   @item.DemoName  <->  @item.CreatedDate </p>


    <p>
        "There tere there"
    </p>
    <div>
        <a asp-action="Detail" asp-controller="Home" asp-route-id="@item.Id">Detail</a>
        @Html.ActionLink("Delete","Delete", new { id = item.Id }, new
               {
                   onclick = "return confirm('Are sure wants to delete?');"
               })
    </div>
}
